Login MVP preparado para conectar con `/v1/auth/login`.

- La pantalla usa `React Hook Form` + `Zod`.
- El flujo mock queda habilitado mientras `VITE_ENABLE_AUTH_MOCK` no sea `false`.
- Credenciales demo:
  - `demo@bmbsalud.cl`
  - `123456`
